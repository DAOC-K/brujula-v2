import { PlaceholderPage } from "@/components/finance/placeholder-page";

export default async function SettingsPage() {
  return (
    <PlaceholderPage
      active="settings"
      kicker="Configuración"
      title="Más"
      description="Aquí estarán perfil, espacios financieros, presupuesto base, preferencias, seguridad y configuración general de Brújula."
    />
  );
}
